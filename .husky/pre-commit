npx lint-staged

# REV-757: secret scan over the staged blobs, via the npm script so the gate is
# reachable by name. The script handles a missing trufflehog itself (warns and
# passes) — a gate nobody can satisfy just gets bypassed, and bypassing hooks is
# not permitted here.
npm run security:secrets
